import React from 'react';

export default () => (
  <nav className="navbar navbar-default">
    <div className="container">
      <button type="button" className="btn btn-success navbar-btn pull-right">Sign in</button>
    </div>
  </nav>
);
